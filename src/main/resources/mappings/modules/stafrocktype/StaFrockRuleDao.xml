<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.rootyu.rad.modules.stafrocktype.dao.StaFrockRuleDao">

	<sql id="StaFrockRuleColumns">
		b.match_principle AS"matchPrinciple",
		b.match_field AS "matchField",
		b.create_by AS"createBy.id",
		b.create_date AS "createDate",
		b.update_by AS"updateBy.id",
		b.update_date AS "updateDate",
		b.remarks AS "remarks",
		b.del_flag AS "delFlag",
		b.id as "id",
		wk.gzzxbm AS "workstationcode"
	</sql>

	<sql id="StaFrockRuleJoins">
		LEFT JOIN llm_sta_frock_rule  b ON b.station = a.id
		LEFT JOIN llm_wk wk ON wk.id=a.pk_wkcenter
	</sql>

	<select id="getStaFrockRuleByStationId" parameterType="String" resultType="StaFrockRule">
		SELECT
		<include refid="StaFrockRuleColumns" />
		FROM llm_wkstadoc  a
		<include refid="StaFrockRuleJoins" />
		<where>
		    a.frocktype = '2'
			and a.del_flag = '0'
			 
			AND a.id = #{station}
		 
		</where>
	</select>

	<select id="findList" resultType="StaFrockRule">
		SELECT
		<include refid="StaFrockRuleColumns" />
			FROM llm_wkstadoc a
		<include refid="StaFrockRuleJoins" />
		<where>
		 	a.frocktype = '2'	
			and a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="station != null and station != ''">
				AND a.workstationcode like '%'||#{station}||'%'
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>

	<insert id="insert">
		INSERT INTO llm_sta_frock_rule(
		id,
		station,
		match_principle,
		match_field,
		create_by,
		create_date,
		update_by,
		update_date,
		remarks,
		del_flag
		) VALUES (
		#{id},
		#{station},
		#{matchPrinciple},
		#{matchField},
		#{createBy.id},
		#{createDate},
		#{updateBy.id},
		#{updateDate},
		#{remarks},
		#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE llm_sta_frock_rule SET 	
			station = #{station},
			match_principle = #{matchPrinciple},
			match_field =  #{matchField},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE llm_sta_frock_rule SET
		del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>

</mapper>