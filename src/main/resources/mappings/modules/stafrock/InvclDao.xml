<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.rootyu.rad.modules.stafrock.dao.InvclDao">
    
	<select id="findList" resultType="cn.rootyu.rad.modules.stafrock.entity.Invcl">
	  select 
		 a.id AS "id",
		 a.INVCLASSNAME AS "invclassname"
	  from llm_invcl a
	  where a.del_flag != #{DEL_FLAG_DELETE}
	  ORDER BY a.id ASC	  
	</select>
	
</mapper>